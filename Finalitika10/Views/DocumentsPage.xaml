<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Finalitika10.Models"
             xmlns:viewmodels="clr-namespace:Finalitika10.ViewModels"
             x:DataType="viewmodels:DocumentsViewModel"
             x:Class="Finalitika10.Views.DocumentsPage" Title="Документы" BackgroundColor="White">
    <CollectionView ItemsSource="{Binding AvailableDocuments}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:DocumentTemplateModel">
                <Grid Padding="20">
                    <Label Text="{Binding Title}" 
                       FontSize="18" 
                       TextColor="Black" 
                       VerticalOptions="Center" />

                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer 
                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenEditorCommand}"
                        CommandParameter="{Binding .}" />
                    </Grid.GestureRecognizers>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>